# Use the official Nginx image from Docker Hub
FROM nginx:alpine

# Copy the custom index.html to the Nginx web root directory
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80 to access the service
EXPOSE 80
